<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".ui.home.HomeMainFragment">

    <include
        android:id="@+id/toolbar_home_main"
        layout="@layout/fragment_common_toolbar"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <androidx.core.widget.NestedScrollView
        android:layout_width="0dp"
        android:layout_height="0dp"
        app:layout_constraintTop_toBottomOf="@id/toolbar_home_main"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="16dp">

            <TextView
                android:id="@+id/textview_home_shortcut_title"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="바로가기"
                android:textSize="18sp"
                android:textStyle="bold"
                android:textColor="@color/black"/>

            <GridLayout
                android:id="@+id/gridlayout_home_shortcuts"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="16dp"
                android:columnCount="3"
                android:useDefaultMargins="true">

                <Button style="@style/Widget.App.Button.Shortcut" android:text="홈페이지" />
                <Button style="@style/Widget.App.Button.Shortcut" android:text="학사일정" />
                <Button style="@style/Widget.App.Button.Shortcut" android:text="학사공지" />
                <Button style="@style/Widget.App.Button.Shortcut" android:text="공지사항" />
                <Button style="@style/Widget.App.Button.Shortcut" android:text="행사공지" />
                <Button style="@style/Widget.App.Button.Shortcut" android:text="장학일정" />
                <Button style="@style/Widget.App.Button.Shortcut" android:text="취업공지" />
                <Button style="@style/Widget.App.Button.Shortcut" android:text="식단표" />
                <Button style="@style/Widget.App.Button.Shortcut" android:text="셔틀버스" />
                <Button style="@style/Widget.App.Button.Shortcut" android:text="AISW공지" />
                <Button style="@style/Widget.App.Button.Shortcut" android:text="SW중심대학" />
            </GridLayout>

            <androidx.constraintlayout.widget.ConstraintLayout
                android:id="@+id/layout_home_recent_header"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="24dp">

                <TextView
                    android:id="@+id/textview_home_recent_title"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="최근 알림"
                    android:textColor="@color/black"
                    android:textSize="18sp"
                    android:textStyle="bold"
                    app:layout_constraintStart_toStartOf="parent"
                    app:layout_constraintTop_toTopOf="parent"
                    app:layout_constraintBottom_toBottomOf="parent"/>

                <TextView
                    android:id="@+id/textview_home_recent_more"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="더보기"
                    android:textSize="14sp"
                    app:layout_constraintEnd_toEndOf="parent"
                    app:layout_constraintTop_toTopOf="parent"
                    app:layout_constraintBottom_toBottomOf="parent"/>

            </androidx.constraintlayout.widget.ConstraintLayout>

            <androidx.recyclerview.widget.RecyclerView
                android:id="@+id/recyclerview_home_notifications"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="8dp"
                tools:listitem="@layout/item_home_notification"
                tools:itemCount="2"/>

        </LinearLayout>
    </androidx.core.widget.NestedScrollView>

</androidx.constraintlayout.widget.ConstraintLayout>